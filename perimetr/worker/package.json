{
  "name": "@perimetr/worker",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply perimetr --local",
    "migrate": "wrangler d1 migrations apply perimetr --remote"
  }
}
